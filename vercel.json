{
  "version": 2,
  "buildCommand": "cd frontend && node ../node_modules/expo/bin/cli export:web && (cp -r ../web-build . 2>/dev/null || true) && (cp -r web-build ../web-build 2>/dev/null || true)",
  "outputDirectory": "frontend/web-build",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
